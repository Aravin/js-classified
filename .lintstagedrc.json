{
  "client/**/*.{js,ts,svelte,json}": [
    "cd client && yarn format",
    "cd client && yarn lint:fix",
    "cd client && yarn run check",
    "cd client && yarn run test"
  ],
  "server/**/*.{js,ts,json,prisma}": [
    "cd server && yarn format",
    "cd server && yarn lint:fix"
  ]
}